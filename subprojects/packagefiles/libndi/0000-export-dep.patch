diff --git a/meson.build b/meson.build
index ebfffbd..31b9302 100644
--- a/meson.build
+++ b/meson.build
@@ -4,7 +4,7 @@ project('libndi', 'c',
                       'warning_level=3',
                       'buildtype=release',
                       'b_ndebug=if-release'],
-  meson_version: '>= 0.47.0')
+  meson_version: '>= 0.54.0')
 
 cc = meson.get_compiler('c')
 
@@ -136,6 +136,14 @@ pkg.generate(
   extra_cflags: ndi_core_export_cflags,
 )
 
+libndi_dep = declare_dependency(
+  compile_args: ndi_core_export_cflags,
+  dependencies: ndi_core_deps,
+  include_directories: include_directories('include'),
+  link_with: libndi,
+)
+
+meson.override_dependency(meson.project_name(), libndi_dep)
 
 #
 # Example Executable
@@ -143,6 +151,8 @@ pkg.generate(
 
 # mDNS discovery specifics
 
+ndi_example_deps += libndi_dep
+
 microdns_dep = dependency('microdns', required: get_option('microdns'))
 if microdns_dep.found()
   threads_dep = dependency('threads', required: false)
@@ -154,7 +164,5 @@ endif
 executable('ndi',
            ndi_example_sources,
            c_args: ndi_example_cargs,
-           include_directories: include_directories('include'),
            install: true,
-           link_with: [libndi],
            dependencies: ndi_example_deps)
